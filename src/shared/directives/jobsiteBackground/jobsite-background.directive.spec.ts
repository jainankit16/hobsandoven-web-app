import { JobsiteBackgroundDirective } from './jobsite-background.directive';

describe('JobsiteBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new JobsiteBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
